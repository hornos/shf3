#!/bin/bash
source $(dirname ${BASH_SOURCE})/../lib/header
import gui
import sys/dir

function help/dirperm() {
  cat << EOF

Usage: ${gbn} directory

EOF
  exit ${_false_}
}

### args
_d="${1:-.}"

### main
echo
msg "Permissions on Directory: ${PWD}"
gui/line

sys/dir/perm "${_d}"
