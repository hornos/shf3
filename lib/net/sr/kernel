
function net/sr() {
  local _s="${1:-julian assange}"
  local _h=${2:-100}
  local _e=${3:-google}
  local _b=${4:-w3m}

  msg "${_s}"
  SURFRAW_browser="${_b}" surfraw -t ${_e} -results=${_h} "${_s}"
}
