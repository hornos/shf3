
function enc/encfs/mgr() {
  import enc/encfs/key

  local _h="${1:-default}"
  local _c="${2:-CREATE}"

  case "${_c}" in
    CREATE)
      enc/encfs/key/gen "${_h}"
      ;;
    CHANGE)
      enc/encfs/key/chg "${_h}"
      ;;
  esac
}

